if(m > 10*f):
            #     fact = m//f - 1
            #     gen += fact
            #     m -= (fact*f)
            # if(f > 10*m):
            #     fact = f//m - 1
            #     gen += fact
            #     f -= (fact*m)